package org.ssd.ledger.blockchain;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class PoSBlockchain extends Blockchain {
    private static final Logger logger = LogManager.getLogger(PoSBlockchain.class);
    public PoSBlockchain() {
        super();
    }
}
